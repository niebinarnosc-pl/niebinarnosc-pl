---
import ContentCard from './ContentCard.astro';
import Icon from './Icon.astro';
import { PortableText } from 'astro-portabletext';
import Thumbnail from './Thumbnail.astro';

interface Props {
    story: any; // TODO: Replace with a proper type from Sanity
    variant?: 'full' | 'summary';
}

const { story, variant = 'full' } = Astro.props;
const isSummary = variant === 'summary';

const href = isSummary ? `/opowiesci#${story.slug.current}` : undefined;
---
<ContentCard id={story.slug.current} href={href}>
  <div slot="thumbnail">
    <Thumbnail image={story.photo} alt={story.title} isSquare isLightbox={!isSummary} />
  </div>
  <div slot="header">
    <h4 class="text-xl font-bold group-hover:text-purple-80 transition-colors">{story.title}</h4>
    {story.definitions && story.definitions.length > 0 && (
        <div class="flex gap-2 overflow-x-auto">
            {story.definitions.map(def => def && isSummary ?
              <span class="badge badge-normal">{def.title}</span> :
              <a class="badge badge-normal" href={`/definicje/${def.slug.current}`}>{def.title}</a>
            )}
        </div>
    )}
  </div>
  <div slot="body">
    {isSummary ? <p>{story.excerpt}</p> : <PortableText value={story.body} />}
  </div>
  <div slot="footer">
      {isSummary && <button class="btn btn-secondary ml-auto">Czytaj więcej <Icon name="arrow-right"/></button>}
  </div>
</ContentCard>
